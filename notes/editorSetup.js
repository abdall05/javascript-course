//strict mode
"use strict";

// use prettier extension to format our code
// then we can configure prettier to change some options
// double quotes to single quotes / arrow function with 1 parameter
// by creating a .prettierrc file in the project folder

//configure snippets : faster way to write repeated code

// install live server to avoid relowding the webpage
// two ways
// 1-vsc extension
// 2-Node.js npm package : npm install live-server -g (globaly)
// then cd to the project foler and run live-server in terminal

debugger; //this will automatically open the debugger in the dev tools of the browser
// will put a breakpoint in that line
